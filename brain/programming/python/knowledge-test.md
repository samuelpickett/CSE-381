# Python Knowledge Test

## Result

Score: 5/5

The user demonstrated a solid understanding of Python fundamentals:

1. Correctly identified lists as mutable and tuples as immutable.
2. Correctly explained that `==` compares values while `is` checks object identity.
3. Correctly described dictionaries as key-value storage and explained key-based access.
4. Correctly distinguished `for` loops from `while` loops, with the nuance that `for` loops iterate over an iterable.
5. Correctly explained exceptions and handling them with `except`.

## Feedback

Excellent foundational knowledge. Continue practicing exception handling with complete `try`/`except` examples and learn about `else` and `finally` clauses.
